<exercises xml:id="s_transformation_ex">
  <subexercises>
    <title>WeBWork Exercises</title>
    <exercise>
      <webwork source="Library/TCNJ/TCNJ_IntroLinearTransformations/problem17.pg" />
    </exercise>

    <exercise>
      <webwork source="Library/Rochester/setLinearAlgebra15TransfOfLinSpaces/ur_la_15_18.pg" />
    </exercise>

    <exercise>
      <webwork source="Library/Rochester/setLinearAlgebra15TransfOfLinSpaces/ur_la_15_17.pg" />
    </exercise>

    <exercise>
      <!-- similar question: Library/Rochester/setLinearAlgebra15TransfOfLinSpaces/ur_la_15_16.pg -->
      <webwork source="Library/Rochester/setLinearAlgebra15TransfOfLinSpaces/ur_la_15_3.pg" />
    </exercise>

    <exercise>
      <!-- similar question: Library/TCNJ/TCNJ_IntroLinearTransformations/problem18.pg -->
      <webwork source="Library/Rochester/setLinearAlgebra14TransfOfRn/ur_la_14_30.pg" />
    </exercise>

    <exercise>
      <!-- similar problem: Library/TCNJ/TCNJ_IntroLinearTransformations/problem1.pg -->
      <!-- similar problem: Library/TCNJ/TCNJ_IntroLinearTransformations/problem2.pg -->
      <!-- similar problem: Library/TCNJ/TCNJ_IntroLinearTransformations/problem3.pg -->
      <!-- similar problem: Library/TCNJ/TCNJ_IntroLinearTransformations/problem4.pg -->
      <!-- similar problem: Library/TCNJ/TCNJ_IntroLinearTransformations/problem6.pg -->
      <!-- similar problem: Library/TCNJ/TCNJ_IntroLinearTransformations/problem7.pg -->
      <!-- similar problem: Library/TCNJ/TCNJ_IntroLinearTransformations/problem8.pg -->
      <webwork source="Library/TCNJ/TCNJ_IntroLinearTransformations/problem15.pg" />
    </exercise>

    <exercise>
      <webwork source="Library/Rochester/setLinearAlgebra15TransfOfLinSpaces/ur_la_15_20.pg" />
    </exercise>
    
  </subexercises>
  <exercise>
    <statement>
      <p>
        For each of the following functions <m>T</m>,
        show that <m>T</m> is nonlinear by providing an explicit counterexample to one of the defining axioms or a consequence thereof.
      </p>
      <ol>
        <li>
          <p>
            <m>T\colon \R^2\rightarrow \R^2</m>,  <m>T((x,y))=(x,y)+(1,1)</m>
          </p>
        </li>
        <li>
          <p>
            <m>T\colon M_{nn}\rightarrow M_{nn}</m>, <m>T(A)=A^2</m>
          </p>
        </li>

        <li>
          <p>
            <m>T\colon M_{nn}\rightarrow \R</m>, <m>T(A)=\det A</m>
          </p>
        </li>
        <li>
          <p>
            <m>T\colon F(\R,\R)\rightarrow F(\R,\R)</m>,
            <m>T(f)=1+f</m>
          </p>
        </li>
        <li>
          <p>
            <m>T\colon\R^3\rightarrow \R^2</m>, <m>T(x,y,z)=(xy,yz)</m>
          </p>
        </li>
      </ol>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_transposition">
    <title>Transposition</title>
    <statement>
      <p>
        Define <m>T\colon M_{mn}\rightarrow M_{nm}</m> as <m>T(A)=A^T</m>: <ie />, the function <m>T</m> takes as input an <m>m\times n</m> matrix and returns as output an <m>n\times m</m> matrix.  Show that <m>T</m> is a linear transformation.
      </p>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_vector_scale">
    <title>Scalar multiplication</title>
    <statement>
      <p>
        Let <m>V</m> be a vector space. Fix <m>c\in \R</m> and define <m>T\colon V\rightarrow V</m> as <m>T(\boldv)=c\boldv</m>: <ie />, <m>T</m> is scalar multiplication by <m>c</m>. Show that <m>T</m> is a linear transformation.
      </p>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_trace">
    <title>Trace</title>
    <statement>
      <p>
      Fix an integer <m>n\geq 1</m>. The <term>trace function</term> is the function <m>\tr\colon M_{nn}\rightarrow \R</m> defined as
      <me>
        \tr A=\sum_{i=1}^n a_{ii}=a_{11}+a_{22}+\cdots +a_{nn}
      </me>.
      Show that the trace function is a linear transformation.
      </p>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_matrix_mult">
    <title>Left/right matrix multiplication</title>
    <statement>
      <p>
      Let <m>B</m> be an <m>r\times m</m> matrix, and let <m>C</m> be an <m>n\times s</m> matrix.
      Define the functions <m>T</m> and <m>S</m> as follows:
        <md>
          <mrow> T\colon M_{mn} \amp\rightarrow M_{rn} </mrow>
          <mrow>A \amp\mapsto T(A)=BA </mrow>
          <mrow>  \amp </mrow>
          <mrow> S\colon M_{mn} \amp\rightarrow M_{ms} </mrow>
          <mrow>  A\amp\mapsto S(A)=AC  </mrow>
        </md>.
        In other words, <m>T</m> is the <q>multiply on the left by <m>B</m></q> operation, and <m>S</m> is the  <q>multiply on the right by C</q> operation
        Show that <m>T</m> and <m>S</m> are linear transformations.
      </p>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_conjugation">
    <title>Conjugation</title>
    <statement>
      <p>
        Fix an invertible matrix <m>Q\in M_{nn}</m>.
        Define <m>T\colon M_{nn}\rightarrow M_{nn}</m> as <m>T(A)=QAQ^{-1}</m>.
        Show that <m>T</m> is a linear transformation.
        This operation is called <term>conjugation by <m>Q</m></term>.
      </p>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_sequence_shift">
    <title>Sequence shift operators</title>
    <statement>
      <p>
        Let <m>V=\R^\infty=\{(a_1,a_2,\dots, )\colon a_i\in\R\}</m>,
        the space of all infinite sequences.
        Define the
        <term>shift left function</term>, <m>T_\ell</m>, and
        <term>shift right function</term>, <m>T_r</m>, as follows:
        <md>
          <mrow>T_\ell\colon \R^\infty\amp \rightarrow \R^\infty \amp  T_r\colon \R^\infty\amp \rightarrow \R^\infty</mrow>
          <mrow>s=(a_1,a_2, a_3,\dots )\amp \longmapsto T_\ell(s)=(a_2, a_3,\dots) \amp  s=(a_1,a_2, a_3,\dots )\amp \longmapsto T_r(s)=(0,a_1,a_2,\dots)</mrow>
        </md>
        Prove that <m>T_\ell</m> and <m>T_r</m> are linear transformations.
      </p>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_function_shift">
    <title>Function shift operators</title>
    <statement>
      <p>
      Fix <m>a\in \R</m>. Define <m>T\colon F(\R,\R)\rightarrow F(\R,\R)</m> as <m>T(f)=g</m>, where <m>g(x)=f(x+a)</m>. Show that <m>T</m> is a linear transformation.
      </p>
    </statement>

  </exercise>
  <exercise xml:id="ex_transformation_function_scale">
    <title>Function scaling operators</title>
    <statement>
      <p>
      Fix <m>c\in \R</m> and define the functions <m>T,S \colon F(\R,\R)\rightarrow F(\R,\R)</m> as follows:
        <md>
          <mrow>T(f) \amp =g, \text{ where } g(x)=f(cx) </mrow>
          <mrow> S(f)\amp =g, \text{ where } g(x)=cf(x)  </mrow>
        </md>.
        Show that <m>T</m> and <m>S</m> are linear transformations.
      </p>
    </statement>

  </exercise>
  <exercise xml:id="ex_transformation_add_scale">
    <title>Adding and scaling linear transformations</title>
    <statement>
      <p>
        Suppose that <m>T\colon V\rightarrow W</m> and <m>S\colon V\rightarrow W</m> are linear transformations.
      </p>
      <ol>
        <li>
          <p>
            Define the function <m>T+S\colon V\rightarrow W</m> as <m>(T+S)(\boldv)=T(\boldv)+S(\boldv)</m>. Show that <m>T+S</m> is a linear transformation.
          </p>
        </li>
        <li>
          <p>
            Define the function <m>cT\colon V\rightarrow W</m> as <m>cT(\boldv)=c(T(\boldv))</m>. Show that <m>cT</m> is a linear transformation.
          </p>
        </li>
      </ol>
    </statement>
  </exercise>
  <exercise>
    <statement>
      <p>
        Let <m>T\colon F(\R,\R)\rightarrow F(\R,\R)</m> be defined as <m>T(f)=g</m>, where <m>g(x)=f(x)+f(-x)</m>. Show that <m>T</m> is linear. You may use the results of <xref ref="ex_transformation_function_scale"/> and <xref ref="ex_transformation_add_scale"/>.
      </p>
    </statement>
  </exercise>
  <exercise>
    <statement>
      <p>
        Prove <xref ref="th_transform_composition"/>.
      </p>
    </statement>
  </exercise>

  <exercise xml:id="ex_transformation_reflection">
    <title>Reflection through a line</title>
    <statement>
      <p>
      Fix an angle <m>\alpha</m> with <m>0\leq \alpha \leq \pi </m>, let <m>\ell_\alpha</m> be the line through the origin that makes an angle of <m>\alpha</m> with the positive <m>x</m>-axis, and let <m>r_\alpha\colon\R^2\rightarrow \R^2</m> be the reflection operation as described in <xref ref="d_reflection"/>.
     Prove that <m>r_\alpha</m> is a linear transformation following the steps below.
        <ol>
          <li>
            <p>
              In a manner similar to <xref ref="th_transform_rotation"/>, describe <m>P'=r_\alpha(P)</m> in terms of the polar coordinates <m>(r,\theta)</m> of <m>P</m>. Additionally, it helps to write <m>\theta=\alpha+\phi</m>, where <m>\phi</m> is the angle the line segment from the origin to <m>P</m> makes with the line <m>\ell_\alpha</m>.
              Include a drawing to support your explanation.
            </p>
          </li>
          <li>
            <p>
              Use your description in (a), along with some trigonometric identities, to show <m>r_\alpha=T_A</m> where
              <me>
                A=\begin{bmatrix}\cos 2\alpha \amp \sin 2\alpha\\ \sin 2\alpha \amp -\cos 2\alpha \end{bmatrix}
              </me>.
            </p>
          </li>
        </ol>
      </p>
    </statement>
  </exercise>
  <exercise xml:id="ex_transformation_composition_rotations_reflections">
    <title>Compositions of rotations and reflections</title>
    <statement>
      <p>
        In this exercise we will show that if we compose a rotation or reflection with another rotation or reflection, as defined in <xref ref="d_rotation"/> and <xref ref="d_reflection"/>, the result is yet another rotation or reflection.
        For each part, express the given composition either as a rotation <m>\rho_\theta</m> or reflection <m>r_\theta</m>, where <m>\theta</m> is expressed in terms of <m>\alpha</m> and <m>\beta</m>.
      </p>
      <ol>
        <li>
          <p>
          <m>\rho_\alpha\circ\rho_\beta</m>
          </p>
        </li>
        <li>
          <p>
          <m>r_\alpha\circ r_\beta</m>
          </p>
        </li>
        <li>
          <p>
          <m>\rho_\alpha\circ r_\beta</m>
          </p>
        </li>
        <li>
          <p>
            <m>r_\alpha\circ \rho_\beta</m>
          </p>
        </li>
      </ol>
    </statement>
    <hint>
      <p>
        Use <xref ref="th_transform_rotation"/> and <xref ref="th_transform_reflection"/>, along with some trigonometric identities.
      </p>
    </hint>

  </exercise>

</exercises>
